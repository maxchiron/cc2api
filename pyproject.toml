[project]
name = "cc2api"
version = "0.1.0"
description = "Gateway that exposes Claude Code CLI as an OpenAI-compatible API"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
]

[project.scripts]
cc2api = "app:main"